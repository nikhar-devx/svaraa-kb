---
name: sort-inbox
description: Find all notes tagged #inbox in content/, classify them, update frontmatter tags, and remove the #inbox tag.
---

Process all `#inbox` notes in `@../content/`.

Follow the full skill definition at `@../.agents/skills/sort-inbox.md`.
